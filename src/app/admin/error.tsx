'use client'

import {Card, CardTitle} from "@/components/ui/card";

export default function Loading (){

    return (
        <Card>
            <CardTitle>⚠ Error ⚠⚠️</CardTitle>
        </Card>
    )
}